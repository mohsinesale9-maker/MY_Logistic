// Legacy module — kept as a no-op so older imports don't break. All real
// configuration now lives in environment variables and is read directly by
// the Supabase client.

export const appParams = {
  appId: "my-logistics",
  token: null,
  functionsVersion: null,
  appBaseUrl: null,
};
